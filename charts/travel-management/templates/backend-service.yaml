apiVersion: v1
kind: Service
metadata:
  name: {{ include "travel-management.fullname" . }}-backend-svc
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.backend.service.port }}
      targetPort: {{ .Values.backend.service.port }}
  selector:
    app: travel-backend
